metadata:
spec:
  containers:
    - name: hub.sinimini.com/docker/docker
      image: docker:dind
      tty: true
      args:
        - dockerd-entrypoint.sh dockerd && cat
      command:
        - "/bin/sh"
        - "-c"
      securityContext:
        privileged: true